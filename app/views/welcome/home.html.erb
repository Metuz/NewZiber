<div class="fullsize fullsize-background"> <!-- Full Size -->
  <div class="container container-inner">
    <div class="row">
      <div class="col-md-6">
        <%= image_tag "12.jpg", :class=> "img-responsive aligncenter" %>
        <img alt="" class="img-responsive aligncenter" src="img/demo/animated-responsive.png" >
      </div>
      <div class="col-md-6">
        <h2 class="title-medium title-color">
          <% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text1, as: :textarea %>
          <% else %>
            <%= @page.text1 %>
          <% end %>
        </h2>
        <p class="lead">
          <% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text2, as: :textarea %>
          <% else %>
            <%= @page.text2 %>
          <% end %>
        </p>
        <div class="row">
          <div class="col-md-6">
            <div class="box-icon-wrapper">
              <h4 class="box-icon-title margin-top20 margin-bottom20">
                <span class="icon-wrapper icon-full-round bg-color-default"><i class="fa fa-medkit color-white"></i></span>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text3, as: :textarea %>
                <% else %>
                  <%= @page.text3 %>
                <% end %>
              </h4>
              <p>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text4, as: :textarea %>
                <% else %>
                  <%= @page.text4 %>
                <% end %>
              </p>
            </div>
          </div>
          <div class="col-md-6">
            <div class="box-icon-wrapper">
              <h4 class="box-icon-title margin-top20 margin-bottom20">
                <span class="icon-wrapper icon-full-round bg-color-purple"><i class="fa fa-book color-white"></i></span>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text5, as: :textarea %>
                <% else %>
                  <%= @page.text5 %>
                <% end %>
              </h4>
              <p>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text6, as: :textarea %>
                <% else %>
                  <%= @page.text6 %>
                <% end %>
              </p>
            </div>
          </div>
        </div>
        <div class="row">
          <div class="col-md-6">
            <div class="box-icon-wrapper">
              <h4 class="box-icon-title margin-top20 margin-bottom20">
                <span class="icon-wrapper icon-full-round bg-color-orange"><i class="fa fa-magic color-white"></i></span>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text7, as: :textarea %>
                <% else %>
                  <%= @page.text7 %>
                <% end %>
              </h4>
              <p>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text8, as: :textarea %>
                <% else %>
                  <%= @page.text8 %>
                <% end %>
              </p>
            </div>
          </div>
          <div class="col-md-6">
            <div class="box-icon-wrapper">
              <h4 class="box-icon-title margin-top20 margin-bottom20">
                <span class="icon-wrapper icon-full-round bg-color-success"><i class="fa fa-tablet color-white"></i></span>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text9, as: :textarea %>
                <% else %>
                  <%= @page.text9 %>
                <% end %>
              </h4>
              <p>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text10, as: :textarea %>
                <% else %>
                  <%= @page.text10 %>
                <% end %>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div><!-- /Full Size -->
<div class="main-wrapper"><!-- Main Container -->
  <div class="container"> <!-- Container -->
    <div class="blank-spacer padding-top20 padding-bottom20"></div>
    <div class="row">
      <div class="col-md-4">
        <div class="service-block sb-border sb-default animation bounceIn">
          <div class="service-block-icon text-center">
            <div class="icon-wrapper icon-border-radius fa-4x color-default">
              <i class="fa fa-paper-plane"></i>
            </div>
          </div>
          <div class="service-block-title text-center">
            <p>Nos lo envias</p>
          </div>
          <div class="service-block-content text-center">
            <p>
              <% if user_signed_in? && current_user.is_a?(Admin) %>
                <%= best_in_place @page, :text11, as: :textarea %>
              <% else %>
                <%= @page.text11 %>
              <% end %>
            </p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="service-block sb-border sb-purple animation bounceIn">
          <div class="service-block-icon text-center">
            <div class="icon-wrapper icon-border-radius fa-4x color-purple">
              <i class="fa fa-cogs"></i>
            </div>
          </div>
          <div class="service-block-title text-center">
            <p>Lo reparamos</p>
          </div>
          <div class="service-block-content text-center">
            <p>
              <% if user_signed_in? && current_user.is_a?(Admin) %>
                <%= best_in_place @page, :text12, as: :textarea %>
              <% else %>
                <%= @page.text12 %>
              <% end %>
            </p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="service-block sb-border sb-orange animation bounceIn">
          <div class="service-block-icon text-center">
            <div class="icon-wrapper icon-border-radius fa-4x color-orange">
              <i class="fa fa-truck"></i>
            </div>
          </div>
          <div class="service-block-title text-center">
            <p>Lo recibes</p>
          </div>
          <div class="service-block-content text-center">
            <p>
              <% if user_signed_in? && current_user.is_a?(Admin) %>
                <%= best_in_place @page, :text13, as: :textarea %>
              <% else %>
                <%= @page.text13 %>
              <% end %>
            </p>
          </div>
        </div>
      </div>
    </div>
    <div class="blank-spacer padding-top20 padding-bottom20"></div>
  </div> <!-- /Container -->
<div class="fullsize fullsize-background padding-medium"> <!-- Full Size -->
  <div class="container"> <!-- Container -->
    <div class="row">
      <div class="col-md-12">
        <h2 class="heading-single text-center title-small title-color">
          <span>
            <% if user_signed_in? && current_user.is_a?(Admin) %>
              <%= best_in_place @page, :text14, as: :textarea %>
            <% else %>
              <%= @page.text14 %>
            <% end %>
          </span>
        </h2>
      </div>
    </div>
    <div class="row">
      <div class="col-md-8 col-md-offset-2">
        <p class="text-center">
          <% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text15, as: :textarea %>
          <% else %>
            <%= @page.text15 %>
          <% end %>
        </p>
      </div>
    </div>
  </div> <!-- /Container -->
  <div  class="caroursel-work"><!-- Carousel -->
    <div class="list_carousel responsive" >
      <div class="carousel-navigation">
        <a id="carou-prev" class="prev" href="#"><i class="fa fa-angle-left"></i></a>
        <a id="carou-next" class="next" href="#"><i class="fa fa-angle-right"></i></a>
      </div>
      <ul class="latest-work disable-select">
        <% @brands.each do |brand| %>
          <li>
            <div class="work-entry">
              <div class="work-entry-content">
                <div class="image-overlay">
                  <%= image_tag brand.logo.url %>
                </div>
                <div class="work-entry-details">
                  <h3><%= brand.name %></h3>
                  <p><%= brand.slogan %></p>
                </div>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
      <div class="clearfix"></div>
    </div>
  </div><!-- /Carousel -->
</div> <!-- /Full Size -->
<div class="container container-inner"><!-- Container -->
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h2 class="heading-single text-center title-small title-color">
        <% if user_signed_in? && current_user.is_a?(Admin) %>
          <%= best_in_place @page, :text16, as: :textarea %>
        <% else %>
          <%= @page.text16 %>
        <% end %>
      </h2>
    </div>
  </div>
    <div class="row">
      <p class="lead">
        <% if user_signed_in? && current_user.is_a?(Admin) %>
          <%= best_in_place @page, :text17, as: :textarea %>
        <% else %>
          <%= @page.text17 %>
        <% end %>
      </p>
      <p class="lead">
        <% if user_signed_in? && current_user.is_a?(Admin) %>
          <%= best_in_place @page, :text18, as: :textarea %>
        <% else %>
          <%= @page.text18 %>
        <% end %>
      </p>
      <p class="lead">
        <% if user_signed_in? && current_user.is_a?(Admin) %>
          <%= best_in_place @page, :text19, as: :textarea %>
        <% else %>
          <%= @page.text19 %>
        <% end %>
      </p>
    </div>
</div><!-- /Container -->
<% if user_signed_in? && current_user.is_a?(Admin) %>
  <%= render 'layouts/footer' %>
<% end %>
