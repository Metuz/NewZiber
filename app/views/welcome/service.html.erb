<div class="fullsize"><!-- Container Tile & Breadcrumbs -->
	<div class="page-header">
		<div class="container">
			<div class="row">
				<div class="col-md-6">
					<h1 class="page-title">Servicios</h1>
				</div><div class="col-md-6">
					<ol class="breadcrumb">
						<li><%= link_to "Home", root_path, :class => "disabled has-submenu" %></li>
						<li>Servicios</li>
					</ol>
				</div>
			</div>
		</div>
	</div>
</div><!-- /Container Tile & Breadcrumbs -->
<div class="main-wrapper"><!-- Main Container -->
	<div class="container container-inner"> <!-- Container -->
		<div class="row">
			<div class="col-md-8">
				<p class="lead">
					<% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text35, as: :textarea %>
          <% else %>
            <%= @page.text35 %>
          <% end %>
				</p>
			</div>
		</div>
		<div class="row">
			<div class="col-md-8 col-md-push-2">
				<hr class="hr-circle-center margin-top40 margin-bottom40">
			</div>
		</div>
		<div class="row">
			<div class="col-sm-6 col-md-4">
				<div class="progress-pie-1">
					<div class="chart-1" data-percent="99">
						<span class="percent"><i class="fa fa-thumbs-o-up"></i></span>
					</div>
				</div>
				<p class="text-center title-color lead">99% Paqueteria</p>
				<p>
					<% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text36, as: :textarea %>
          <% else %>
            <%= @page.text36 %>
          <% end %>
				</p>
			</div>
			<div class="col-sm-6 col-md-4">
				<div class="progress-pie-2">
					<div class="chart-2" data-percent="2.5">
						<span class="percent">2.5%</span>
					</div>
				</div>
				<p class="text-center title-color lead">Defectuoso</p>
				<p>
					<% if user_signed_in? && current_user.is_a?(Admin) %>
            <%= best_in_place @page, :text37, as: :textarea %>
          <% else %>
            <%= @page.text37 %>
          <% end %>
				</p>
			</div>
			<div class="col-sm-6 col-md-4">
				<div class="progress-pie-3">
					<div class="chart-3" data-percent="89">
						<span class="percent"><i class="fa fa-rocket"></i></span>
					</div>
				</div>
				<p class="text-center title-color lead">89% Satisfaccion</p>
				<% if user_signed_in? && current_user.is_a?(Admin) %>
					<%= best_in_place @page, :text38, as: :textarea %>
				<% else %>
					<%= @page.text38 %>
				<% end %>
			</div>
		</div>
	</div> <!-- /Container -->
</div><!-- /Main Container -->
