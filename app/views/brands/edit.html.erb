<div class="fullsize">
  <div class="page-header">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <h1 class="page-title"><%= @brand.name %></h1>
        </div>
        <div class="col-md-6">
          <ol class="breadcrumb">
            <li><%= link_to "Marcas", brands_path, :class => "disabled has-submenu" %></li>
            <li>Editar</li>
            <li><%= @brand.name %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="main-wrapper">
  <div class="container container-inner"> <!-- Container -->
    <div class="row">
      <div class="col-md-12">
        <%= render 'form' %>
        <% if can? :show, @brand %>
          <p><%= link_to 'Ver', @brand %></p>
        <% end %>
        <p><%= link_to 'Regresar', brands_path %></p>
      </div>
    </div>
  </div>
</div>
