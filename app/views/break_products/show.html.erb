<div class="fullsize">
  <div class="page-header">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <h1 class="page-title"><%= @break_product.serial %></h1>
        </div>
        <div class="col-md-6">
          <ol class="breadcrumb">
            <li><%= link_to "Marcas", brands_path, :class => "disabled has-submenu" %></li>
            <li><%= @brand.name %></li>
            <% if can? :update, @brand %>
              <li><%= link_to 'Editar', edit_brand_path(@brand) %></li>
            <% end %>
            <% if can? :destroy, @brad %>
              <li><%= link_to 'Eliminar',  @brand, :confirm => 'Seguro?', :method => :delete %></li>
            <% end %>
          </ol>
        </div>
      </div>
    </div>
  </div>
</div>



<p id="notice"><%= notice %></p>

<p>
  <strong>Model:</strong>
  <%= @break_product.model %>
</p>

<p>
  <strong>Serial:</strong>
  <%= @break_product.serial %>
</p>

<p>
  <strong>Brand:</strong>
  <%= @break_product.brand_id %>
</p>

<p>
  <strong>Report:</strong>
  <%= @break_product.report_id %>
</p>

<%= link_to 'Edit', edit_break_product_path(@break_product) %> |
<%= link_to 'Back', break_products_path %>
