<div class="footer-wrapper">
  <!-- Footer Middle Container -->
  <div class="footer-middle">
    <div class="container"> <!-- Container -->
      <div class="row">
        <div class="col-md-4">
          <div class="footer-widget">
            <h3>Conocenos</h3>
            <p class="lead">"<em>
              <% if user_signed_in? && current_user.is_a?(Admin) %>
                <%= best_in_place @page, :text20, as: :textarea %>
              <% else %>
                <%= @page.text20 %>
              <% end %>
            </em>"</p>
            <p>
              <% if user_signed_in? && current_user.is_a?(Admin) %>
                <%= best_in_place @page, :text21, as: :textarea %>
              <% else %>
                <%= @page.text21 %>
              <% end %>
            </p>
          </div>
        </div>
        <div class="col-md-4">
          <div class="footer-widget">
            <h3>Contactanos</h3>
            <ul class="fa-ul">
              <li><i class="fa-li fa fa-map-marker"></i>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text22, as: :textarea %>
                <% else %>
                  <%= @page.text22 %>
                <% end %>
              </li>
  						<li><i class="fa-li fa fa-phone"></i>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text23, as: :textarea %>
                <% else %>
                  <%= @page.text23 %>
                <% end %>
              </li>
  						<li><i class="fa-li fa fa-envelope"></i>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text24, as: :textarea %>
                <% else %>
                  <%= @page.text24 %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
        <div class="col-md-4">
          <div class="footer-widget">
            <h3>Politicas</h3>
            <ul class="footer-custom-menu">
              <li>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text25, as: :textarea %>
                <% else %>
                  <%= @page.text25 %>
                <% end %>
              </li>
              <li>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text26, as: :textarea %>
                <% else %>
                  <%= @page.text26 %>
                <% end %>
              </li>
              <li>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text27, as: :textarea %>
                <% else %>
                  <%= @page.text27 %>
                <% end %>
              </li>
              <li>
                <% if user_signed_in? && current_user.is_a?(Admin) %>
                  <%= best_in_place @page, :text28, as: :textarea %>
                <% else %>
                  <%= @page.text28 %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div> <!-- /Container -->
	</div>
	<!-- /Footer Middle Container -->
	<!-- Footer Bottom Container -->
  <div class="footer-bottom">
    <div class="fullsize container"> <!-- Container -->
      <div class="row">
        <div class="col-md-6">
          <div class="logo-bottom">
            <%= image_tag("logo.png", :style => "width: 150px; height:150px;", :alt=>"NewZiber Logo")%>
          </div>
        </div>
        <div class="col-md-6">
          <ul class="footer-social list-inline">
            <li><a href="http://www.facebook.com/newziber.godoy "><div class="icon-wrapper icon-border-round fa-2x"><i class="fa fa-facebook"></i></div></a></li>
            <li><a href="http://www.twitter.com"><div class="icon-wrapper icon-border-round fa-2x"><i class="fa fa-skype"></i></div></a></li>
          </ul>
        </div>
      </div>
      <div class="row">
        <div class="col-md-6 col-md-push-6">
          <ul class="footer-menu list-inline">
          </ul>
        </div>
        <div class="col-md-6 col-md-pull-6">
          <div class="copyright">© 2015 New Ziber. All Rights Reserved.</div>
        </div>
      </div>
    </div> <!-- /Container -->
  </div><!-- /Footer Bottom Container -->
</div><!-- /Footer Container -->
<!-- Back To Top -->
  <a href="#" class="scrollup"><i class="fa fa-angle-up"></i></a>
<!-- /Back To Top -->
