<div class="fullsize">
  <div class="page-header">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <h1 class="page-title"><%= @category.name %></h1>
        </div>
        <div class="col-md-6">
          <ol class="breadcrumb">
            <li><%= link_to "Categorias", categories_path, :class => "disabled has-submenu" %></li>
            <li>Editar</li>
            <li><%= @category.name %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="main-wrapper">
  <div class="container container-inner"> <!-- Container -->
    <div class="row">
      <div class="col-md-12">
        <%= render 'form' %>
        <% if can? :show, @category %>
          <p><%= link_to 'Ver', @category %></p>
        <% end %>
        <p><%= link_to 'Regresar', categories_path %></p>
      </div>
    </div>
  </div>
</div>
