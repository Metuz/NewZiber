<h1>Reportes Sucursal <%= current_user.location.name %></h1>
<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Clave</th>
        <th>Clente</th>
        <th>Tecnico</th>
        <th>Recibido</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      <% @reports.each do |report| %>
        <tr>
          <td><%= link_to report do %><i class="fa fa-eye"></i><% end %><%= report.pin %></td>
          <td><%= report.client.clientname %></td>
          <td><%= report.is_assing? %></td>
          <td><%= report.is_delivered? %></td>
          <td><%= report.fninished %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
